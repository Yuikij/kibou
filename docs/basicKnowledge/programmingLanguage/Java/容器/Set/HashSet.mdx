> extends AbstractSet\<E> implements Set\<E>, Cloneable, java.io.Serializable

基于哈希表实现，用于快速查找和添加，没有索引的概念，无序的，就是说添加的元素排在哪个地方相对来说是未知的。  
add, remove 和 contains 都是近似 O(1) 的复杂度。

* #### 容器实体

```java
    private transient HashMap<E,Object> map;
```